@{
  Layout = "_Layout";
}
@using AirPet.Models;
@model List< AirPet.Models.Profile>;
 <div class="hero-image">
    <div class="hero-text">
      <p>@Html.ActionLink("Create Profile", "Create")</p></h1>
@foreach(Profile profile in Model)
{
  @Html.ActionLink("My Profile", "Details", new { id = profile.ProfileId })
}
    </div>
  </div>
@* <h1>Profile Index</h1> *@
